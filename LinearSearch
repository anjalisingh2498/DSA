int LinearSearch(int arr[], int n, int key){
  for(int i=0;i<n;i++){
    arr[i]==key;
    return i;
  }
return -1;
}
int main(){
  int n;
  cin>>n;
  int arr[n];
  for(int i=0;i<n;i++){
      cin>>arr[i];
  }

  int key;
  cin>>key;
  for(int i=0;i<=n;i++){
      cout<<arr[i]<<" ";
  }
return 0;
}
